# Adapter: Diamond Config

## Mock Injection
- Use HSF call `TestService.updateDiamondConfig` to inject mock values dynamically.
- **Parameters**: `dataId`, `groupId`, `changeKey`, `changeValue`.

## Fallback
- If dynamic injection fails, guide user to manual console update.
